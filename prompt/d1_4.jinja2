{# context: class_name, focal_method, information, contract_text (optional) #}
Greetings! Thank you for assisting me in crafting the unit test. Your expertise is invaluable in generating test cases targeting specific segments of the method-under-test. Let's begin by introducing the method to be tested. Then, detailed instructions will follow for generating the test case. Finally, examples will illustrate how to utilize the method-under-test and compose corresponding test cases.
Please write a JUnit 5 test class for the Java method described below.

### Method-to-test && Dependencies
**Introduction of method-to-test (`{{ focal_method }}`) and Focal Class (`{{ class_name }}`)**

The method-to-test, `{{ focal_method }}`, resides in the focal class `{{ class_name }}`.

Here is the source code of the focal class, including member methods and fields.
```java
{{ information }}
```
{% if contract_text %}
Here, we provide information related to program contracts, which can offer core details such as input-output constraints of methods, parameter value ranges, exception-triggering conditions, and expected behavior specifications, so as to establish clear boundaries and verification criteria for test case generation.
{{ contract_text }}
{% endif %}

Now please generate a whole unit test file for the method-to-test.

#### Requirements and Attention for the Unit Test to Generate:
- Class name: `{{ class_name }}Test`
- Ensure that the unit tests are executable: they should run without any compilation errors, runtime errors, or timeouts.
- Aim for comprehensive coverage: the unit tests should encompass a significant portion of the codebase, including instructions and branches within the method under test.
- Avoid altering the method under test.
- Generate a complete unit test file, including the package declaration and all imports.
- Import all dependent libraries used in the unit test file.
- Use JUnit 5 (`@Test`, `Assertions.*`) and Mockito if needed.
- Each test name must be descriptive (e.g., `test{{ focal_method }}_nullInput`).
- All @Test methods must declare `throws Exception`.
- Identify Abstraction: Before instantiating any class, check if it is marked as abstract in the provided context or common knowledge.
- Strict Prohibition: NEVER use the new keyword to directly instantiate an abstract class (e.g., new AbstractClass(...) is FORBIDDEN).
- NEVER use anonymous inner class syntax for final classes (e.g., new FinalClass(...) { ... } is a syntax error).
- Only use APIs that appear in the provided class context above.
{% if contract_text %}
- CRITICAL: Your tests MUST verify the postconditions listed in the contract above.
- CRITICAL: Generate at least one test for each precondition violation (using `assertThrows`).
{% endif %}
- Output **ONLY** the complete Java class — no prose, no markdown fences.
### Output Format:

Here are my requirements for your output format:

<generate>
The whole unit test file is:
```java
...
```
</generate>

Now, armed with this information, please proceed with the generation following my instructions.
You MUST finish all generation in ONE RESPONSE!
You MUST FULLY write ALL test methods!
You shouldn't leave any spare work for the human! Finish everything!