Please help me generate a whole JUnit test for a focal method in a focal class.
I will provide the following information of the focal method:
1. Required dependencies to import.
2. The focal class signature.
3. Source code of the focal method.
4. Signatures of other methods and fields in the class.
I will provide following brief information if the focal method has dependencies:
1. Signatures of dependent classes.
2. Signatures of dependent methods and fields in the dependent classes.
I need you to create a whole unit test using Junit 5 and Mockito 3, ensuring optimal branch and line coverage. The whole test should include necessary imports for Junit 5 and Mockito 3, compile without errors, and use reflection to invoke private methods. No additional explanations required.

## CRITICAL COMPILATION RULES — You MUST follow ALL of these or the test will fail to compile:

### Rule 1: Private Field/Method Access — Use Reflection ONLY
NEVER access private fields or methods directly (e.g., `obj.privateField` or `obj.privateMethod()`).
ALWAYS use reflection:
```java
Field field = ClassName.class.getDeclaredField("fieldName");
field.setAccessible(true);
Object value = field.get(instance);
```
For private methods:
```java
Method method = ClassName.class.getDeclaredMethod("methodName", ParamType.class);
method.setAccessible(true);
Object result = method.invoke(instance, args);
```

### Rule 2: No Extending/Subclassing final Classes
NEVER write `new FinalClass(...) { ... }` (anonymous subclass).
If the class is `final`, you CANNOT subclass it. Use Mockito spy instead:
```java
FinalClass spy = Mockito.spy(new FinalClass(...));
```

### Rule 3: Type Compatibility for Constructor Arguments
ALWAYS check the actual constructor signature before passing arguments.
- `CSVPrinter(Appendable out, CSVFormat format)` — `out` must implement `Appendable`
  → Use `new StringBuilder()`, `new StringWriter()`, or `new PrintWriter(...)`, NOT `Closeable`/`Flushable` alone.
- If you need a mock that also implements `Appendable`, declare it as:
  ```java
  Appendable mockOut = mock(Appendable.class);
  ```

### Rule 4: Checked Exceptions Must Be Declared
If a Mockito stub call (e.g., `when(rs.next()).thenThrow(...)`) is inside a method that doesn't declare `throws SQLException`, wrap it in try-catch OR add `throws Exception` to the test method signature:
```java
@Test
void testSomething() throws Exception {
    when(rs.next()).thenReturn(true);
}
```
ALWAYS add `throws Exception` to ALL @Test methods that involve checked exceptions.

### Rule 5: Ambiguous Method Overload — Cast the Argument
When calling an overloaded method like `printRecords(null)` where multiple overloads match `null`, CAST explicitly:
```java
printer.printRecords((Object[]) null);   // selects Object[] overload
printer.printRecords((ResultSet) null);  // selects ResultSet overload
```

### Rule 6: No API That Does Not Exist in This Version
NEVER use `CSVFormat.Builder` — this API does not exist in Defects4J Csv versions.
ALWAYS use the static factory methods and chained setters that existed in older versions:
```java
CSVFormat format = CSVFormat.DEFAULT.withDelimiter(',').withQuote('"');
```
Do NOT assume modern builder patterns exist. Use only what appears in the provided class signatures.

### Rule 7: Exception Type Precision
When using `assertThrows`, use the EXACT exception type that the method actually throws — NOT a parent class.
If the method throws `IndexOutOfBoundsException`, do NOT assert `StringIndexOutOfBoundsException`.
Use `assertThrows(IndexOutOfBoundsException.class, ...)` or the common parent `RuntimeException.class` when unsure.

### Rule 8: Correct assertThrows Lambda Syntax
The second argument to `assertThrows` must be an `Executable` (no-arg lambda):
```java
// CORRECT:
assertThrows(IOException.class, () -> reader.lookAhead());
// WRONG:
assertThrows(IOException.class, e -> reader.lookAhead());  // 'e' parameter is wrong
```

### Rule 9: Assertion Value Accuracy
Do NOT assert values based on assumptions. Only assert values you can derive from the source code.
When in doubt, use a weaker assertion:
- Instead of `assertEquals(42, result)`, use `assertNotNull(result)` or `assertTrue(result >= 0)`.
- For END_OF_STREAM/UNDEFINED constants, use the constant name: `assertEquals(ExtendedBufferedReader.END_OF_STREAM, actual)`.

### Rule 10: String vs toString()
When comparing method output to a String, ensure the assertion type matches:
```java
// If result is List<String>, do NOT assertEquals("only,one,record", result)
// Instead: assertEquals(List.of("only", "one", "record"), result)
//      or: assertEquals("only,one,record", String.join(",", result))
```

### Rule 11: Constructor Argument Count
ALWAYS check the constructor signature in the provided class context before calling it.
Do NOT pass more or fewer arguments than the constructor accepts.