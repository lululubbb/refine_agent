You are an expert Java developer. Fix the following Test file based on the repair instructions.

## Test File: `{{ test_name }}.java`


## Unit Test to Fix
Here's the unit test that needs fixing. .
```java
{{ current_suite }}
```

{# ══════════════════════════════════════════════════════════════ #}
{# 编译/运行错误区块（仅在 use_compile_exec=True 时显示）        #}
{# ══════════════════════════════════════════════════════════════ #}
{% if use_compile_exec is not defined or use_compile_exec %}

{% if not compile_ok %}
## COMPILE ERRORS MESSAGE
```
{% for err in compile_errors %}
{{ err }}
{% endfor %}
```


{% elif not exec_ok %}
## RUNTIME ERRORS MESSAGE
```
{% for err in exec_errors %}
{{ err }}
{% endfor %}
```


{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════════ #}
{# 覆盖率区块（仅在 use_coverage=True 且编译/执行通过时显示）    #}
{# ══════════════════════════════════════════════════════════════ #}
{% if (use_coverage is not defined or use_coverage) and (compile_ok and exec_ok) %}

{% if missed_methods or partial_methods %}
## COVERAGE ISSUES (tests compile and run, but focal method coverage is low)
To improve coverage, you should add new test methods that cover the missing lines and branches in the focal method.
{% if missed_methods %}
**Completely uncovered methods — add `@Test` methods to cover these:**
{% for m in missed_methods %}
- {{ m }}
{% endfor %}
{% endif %}

{% if partial_methods %}
**Partially covered methods — add tests for the missing branches:**
{% for m in partial_methods %}
- {{ m }}
{% endfor %}
{% endif %}


{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════════ #}
{# Bug Revealing 区块（仅在 use_bug_revealing=True 且编译/执行通过时显示时显示）      #}
{# ══════════════════════════════════════════════════════════════ #}
{% if (use_bug_revealing is not defined or use_bug_revealing) and (compile_ok and exec_ok)%}

{% if "NOT_BUG_REVEALING" in issues %}
## BUG REVEALING — Tests pass/fail on both buggy and fixed versions (not revealing the bug)
you should strengthen the assertions to reveal the bug.


{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════════ #}
{# 冗余度区块（仅在 use_redundancy=True 且编译/执行通过时显示时显示）                 #}
{# ══════════════════════════════════════════════════════════════ #}
{% if (use_redundancy is not defined or use_redundancy) and (compile_ok and exec_ok) %}

{% if "HIGH_REDUNDANCY" in issues %}

## HIGH REDUNDANCY - AST-level combined similarity metrics
you should minimize structural and behavioral similarity between test cases.

{% endif %}
{% endif %}

## Suite Assessment 
This is the quality assessment for the aforementioned unit test.
{{ suite_summary }}

## Repair Instructions for THIS Test File

```json
{{ instructions_json }}
```

## the source code of the method under test and its classis:
```java
{{ method_code }}
```

# Procedures for Fixing the Unit Test:
Let's proceed step by step:
1. Pick out the statements that the errors occur
2. Explain the causes of the errors
3. Give solutions on how to fix the errors
3. Provide the complete fixed unit test, utilizing JUnit 5.

# Requirements and Considerations for the Unit Test Fix:
- Keep the class name exactly: `{{ test_name }}`
- Ensure the unit tests are executable without compile errors, runtime errors, or timeouts.
- Avoid modifying the method under test.
- **ALL `@Test` method signatures MUST include `throws Exception`.**
- Generate the entire unit test file, including package declaration and imports.
- Only use APIs that appear in the Focal Method Context above; do NOT invent APIs.
- Identify Abstraction: Before instantiating any class, check if it is marked as abstract in the provided context or common knowledge.


Please fix the error and return the whole fixed unit test. You can use Junit 5, Mockito 3 and reflection. No explanation is needed.