You are an expert Java developer. Fix the following Test file based on the repair instructions.
You MUST follow ALL compilation rules below or the test will STILL fail to compile.

## Test File: `{{ test_name }}.java`

## Focal Method Context
```java
{{ method_code }}
```
{% if contract_text %}

{{ contract_text }}
{% endif %}

## Current Test File Source
```java
{{ current_suite }}
```

## Suite Assessment (context)
{{ suite_summary }}

## Repair Instructions for THIS Test File
```json
{{ instructions_json }}
```

## Relevant Imports (add if missing)
```java
{{ imports }}
```

{% if not compile_ok %}
## COMPILE ERRORS — MUST FIX ALL OF THESE FIRST (test will not run until resolved)
```
{% for err in compile_errors %}
{{ err }}
{% endfor %}
```

**Error Type Classification:** {{ error_summary }}


{% elif not exec_ok %}
## RUNTIME ERRORS — MUST FIX (tests compile but fail at runtime)
```
{% for err in exec_errors %}
{{ err }}
{% endfor %}
```


{% else %}
## COVERAGE ISSUES (tests compile and run, but coverage is low)
{% if missed_methods %}
**Completely uncovered methods — add @Test methods for these:**
{% for m in missed_methods %}
- {{ m }}
{% endfor %}
{% endif %}
{% if partial_methods %}
**Partially covered — add tests for missing branches:**
{% for m in partial_methods %}
- {{ m }}
{% endfor %}
{% endif %}
{% endif %}

## BUG REVEALING ISSUES
Strengthen assertions to detect the specific bug:
- Add assertions for exact return values (not just `assertNotNull`)
- Test boundary values that exercise the fixed code path
- Use multiple consecutive calls to check stateful behavior
{% if contract_text %}
- Verify postconditions from the Program Contract
{% endif %}

## HIGH REDUNDANCY
Replace with a completely different test scenario (different input, different code path).


## Rules
1. Apply ALL repair instructions.
2. Keep the class name exactly: `{{ test_name }}`
3. Do NOT change @Test methods that are NOT mentioned in instructions.
4. Add missing imports.
5. The output MUST be compilable Java.
6. Only use APIs that appear in the Focal Method Context above.
{% if contract_text %}
7. At least one test must verify each postcondition from the Program Contract.
{% endif %}
8. When using Mockito, do NOT mock final classes or final methods. Every mocked call must have a corresponding thenReturn, thenThrow, or thenAnswer.
9. All test method signatures MUST include `throws Exception`.
10. Output ONLY the complete Java class — no prose, no markdown fences.
