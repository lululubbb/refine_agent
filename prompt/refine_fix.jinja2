You are an expert Java developer. Fix the following Test file based on the repair instructions.
You MUST follow ALL compilation rules below or the test will STILL fail to compile.

## Test File: `{{ test_name }}.java`

## Focal Method Context
```java
{{ method_code }}
```
{% if contract_text %}

{{ contract_text }}
{% endif %}

## Current Test File Source
```java
{{ current_suite }}
```

## Suite Assessment (context)
{{ suite_summary }}

## Repair Instructions for THIS Test File
```json
{{ instructions_json }}
```

## Relevant Imports (add if missing)
```java
{{ imports }}
```

{# ══════════════════════════════════════════════════════════════ #}
{# 编译/运行错误区块（仅在 use_compile_exec=True 时显示）        #}
{# ══════════════════════════════════════════════════════════════ #}
{% if use_compile_exec is not defined or use_compile_exec %}

{% if not compile_ok %}
## ❌ COMPILE ERRORS — MUST FIX ALL OF THESE FIRST (test will not run until resolved)

The following errors were reported by the Java compiler. Read each error carefully and fix ALL of them:

```
{% for err in compile_errors %}
{{ err }}
{% endfor %}
```


{% elif not exec_ok %}
## ❌ RUNTIME ERRORS — MUST FIX (tests compile but fail at runtime)

The following errors occurred when running the test. Read each error carefully:

```
{% for err in exec_errors %}
{{ err }}
{% endfor %}
```


{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════════ #}
{# 覆盖率区块（仅在 use_coverage=True 且编译/执行通过时显示）    #}
{# ══════════════════════════════════════════════════════════════ #}
{% if (use_coverage is not defined or use_coverage) and (compile_ok and exec_ok) %}

{% if missed_methods or partial_methods %}
## 📊 COVERAGE ISSUES (tests compile and run, but focal method coverage is low)

{% if missed_methods %}
**Completely uncovered methods — add `@Test` methods to cover these:**
{% for m in missed_methods %}
- {{ m }}
{% endfor %}
{% endif %}

{% if partial_methods %}
**Partially covered methods — add tests for the missing branches:**
{% for m in partial_methods %}
- {{ m }}
{% endfor %}
{% endif %}


{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════════ #}
{# Bug Revealing 区块（仅在 use_bug_revealing=True 时显示）      #}
{# ══════════════════════════════════════════════════════════════ #}
{% if use_bug_revealing is not defined or use_bug_revealing %}

{% if "NOT_BUG_REVEALING" in issues %}
## 🐛 BUG REVEALING — Tests pass on both buggy and fixed versions (not revealing the bug)


{% if contract_text %}
- **Verify postconditions from the Program Contract**: The contract lists specific behaviors that must hold
{% endif %}
{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════════ #}
{# 冗余度区块（仅在 use_redundancy=True 时显示）                 #}
{# ══════════════════════════════════════════════════════════════ #}
{% if use_redundancy is not defined or use_redundancy %}

{% if "HIGH_REDUNDANCY" in issues %}
## 🔁 HIGH REDUNDANCY — This test is too similar to another test in the suite

{% endif %}
{% endif %}

## Rules
1. Apply ALL repair instructions listed above.
2. Keep the class name exactly: `{{ test_name }}`
3. Do NOT change `@Test` methods that are NOT mentioned in the instructions (unless fixing compilation).
4. Add any missing `import` statements.
5. The output MUST be compilable Java — no syntax errors.
6. Only use APIs that appear in the Focal Method Context above; do NOT invent APIs.
{% if contract_text %}
7. At least one test must verify each postcondition from the Program Contract.
{% endif %}
8. When using Mockito: do NOT mock final classes; every `when(...).thenXxx(...)` must have a matching stub.
9. **ALL `@Test` method signatures MUST include `throws Exception`.**
10. Output **ONLY** the complete Java class — no prose, no markdown fences, no explanations.
