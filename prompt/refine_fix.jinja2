You are an expert Java developer. Fix the following Test file based on the repair instructions.

## Test File: `{{ test_name }}.java`

## Unit Test to Fix
```java
{{ current_suite }}
```

{# ══════════════════════════════════════════════════════════ #}
{# Compile errors: ONLY shown when strategy is "compile"     #}
{# All other context is suppressed to avoid LLM confusion    #}
{# ══════════════════════════════════════════════════════════ #}
{% if use_compile_exec and not compile_ok %}
## COMPILE ERRORS — Fix ONLY these. 
```
{% for err in compile_errors %}{{ err }}
{% endfor %}```
{% endif %}

{# ══════════════════════════════════════════════════════════ #}
{# Runtime errors: ONLY shown when strategy is "exec"        #}
{# ══════════════════════════════════════════════════════════ #}
{% if use_compile_exec and compile_ok and not exec_ok %}
## RUNTIME ERRORS — Fix the execution failure. 
```
{% for err in exec_errors %}{{ err }}
{% endfor %}```
{% endif %}

{# ══════════════════════════════════════════════════════════ #}
{# Branch hints: shown when strategy is "branch_coverage"    #}
{# Contains specific uncovered line numbers from JaCoCo XML  #}
{# ══════════════════════════════════════════════════════════ #}
{% if use_coverage and compile_ok and exec_ok %}
{% if branch_hint_text %}
{{ branch_hint_text }}
{% elif focal_line_covered is not none and focal_line_total is not none and focal_line_total > 0 %}
## COVERAGE ISSUES — Focal method line coverage: {{ focal_line_covered }}/{{ focal_line_total }} ({{ "%.0f"|format(focal_line_covered / focal_line_total * 100) }}%)
{{ focal_line_total - focal_line_covered }} lines in the focal method are NOT executed.
{% if focal_branch_rate is not none and focal_branch_rate < 80 %}
Branch coverage: {{ "%.0f"|format(focal_branch_rate) }}% — some conditional branches are not taken.
{% endif %}
Add @Test methods targeting the specific uncovered paths in the focal method.
{% endif %}
{% endif %}

{# ══════════════════════════════════════════════════════════ #}
{# Assertion hardening: strategy is "assertion_hardening"    #}
{# Only when coverage is sufficient but test is not revealing #}
{# ══════════════════════════════════════════════════════════ #}
{% if use_bug_revealing and compile_ok and exec_ok %}
## BUG REVEALING — Coverage is sufficient but assertions are too weak

The test reaches the focal method but cannot distinguish buggy from fixed behavior.

Priority actions (in order):
1. Replace `assertNotNull(result)` with `assertEquals(exactExpectedValue, result)`
2. Trace the focal method logic manually to determine the exact expected value
3. Assert specific numeric values, string contents, or state changes
4. Focus on boundary values where the bug is most likely triggered
{% endif %}

{# ══════════════════════════════════════════════════════════ #}
{# Redundancy: strategy is "diversify"                        #}
{# ══════════════════════════════════════════════════════════ #}
{% if use_redundancy and compile_ok and exec_ok %}
## HIGH REDUNDANCY — Rewrite to target a different code path
Remove the most structurally similar test and replace with one targeting a different branch or input category.
{% endif %}

{# ══════════════════════════════════════════════════════════ #}
{# Strategy rationale (from CoupledScore)                    #}
{# ══════════════════════════════════════════════════════════ #}

## Repair Instructions
```json
{{ instructions_json }}
```

## Focal method source
```java
{{ method_code }}
```

## Requirements
- Class name MUST be exactly: `{{ test_name }}`
- {{ count_rationale if count_rationale else "Maintain the same number of @Test methods." }}
- ALL `@Test` methods MUST declare `throws Exception`
- Include complete package declaration and all imports
- Only use APIs present in the focal method source above
- Output ONLY the complete Java class, no explanations
- Ensure that the unit tests are executable: they should run without any compilation errors, runtime errors, or timeouts.
